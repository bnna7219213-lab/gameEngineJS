# Browser Game Engine — PRD 一页纸

> 产品需求文档 · 2026-09-04 · v2

---

## 一句话定义

**一个纯浏览器运行的完整 3D 游戏引擎**——替代 Three.js 做渲染、替代 Unity 做完整开发闭环（编辑器+运行时+导出+协作），零后端算力、零运行时依赖、无构建步骤。

---

## 目标用户

| 用户群 | 痛点 | 我们的解 |
|---|---|---|
| 独立游戏开发者 | Unity 需要付费 + 打包慢 + 后端依赖 | 浏览器打开即用，免费，构建=导出目录 |
| 教育/教学 | 学生装环境痛苦，Unity 太重 | 零安装，浏览器打开写代码跑游戏 |
| 原型验证 | 快速验证游戏机制，不想搭服务器 | 浏览器内完成全部计算，无后端 |
| 嵌入式/H5 游戏 | WebGL 性能受限，Three.js 功能不够 | 自研 WebGL2/WebGPU 引擎，3D 物理完整 |

---

## 核心功能（v2 完整清单）

### 引擎运行时（已交付 ✅）
| 功能 | 状态 | 说明 |
|---|---|---|
| 渲染核心 | ✅ | WebGL2 RHI（布局/uniform/纹理/FBO/MRT/MSAA/instancing）+ Software 黄金参考 |
| PBR 光照 | ✅ | GGX / PCF 阴影 / IBL / 雾 / 后处理链 / 文字图集 |
| 物理 3D | ✅ | GJK+EPA / Sequential Impulse / 多点接触 / 休眠 / Raycast / 角色控制器 |
| 内容管线 | ✅ | GLTF 2.0（.glb）/ 骨骼动画+mixer / 图片解码 / DDC / 纹理桥 |
| 平台层 | ✅ | OPFS 持久化 / FSA / Worker 池（协议层）/ 资源缓存 |
| 推理 | ✅ | 神经网络推理模块 |

### 编辑器（已交付 ✅）
| 功能 | 状态 | 说明 |
|---|---|---|
| GPU 视口 | ✅ | 实时渲染 + Gizmo + 网格/坐标轴 |
| 层级/检视器 | ✅ | Transform / Material / Particle 属性编辑 |
| 代码工作台 | ✅ | Worker 语法校验 / tokenizer 高亮 / 多 Tab |
| Play 模式 | ✅ | 暂停 / 单步 / 热重载 / 断点 / 监视表达式 |
| Profiler | ✅ | 帧时间线 / Draw Call 统计 / 内存面板 |
| Debug 模式 | ✅ | 断点 / 单步 / 表达式 |

### 待交付（v2 Q2-Q7）
| 功能 | 阶段 | 说明 |
|---|---|---|
| 音频输出 | Q2 | WebAudio 桥 / 合成器 / 3D Panner / 文件解码 |
| 网络传输 | Q2 | WebSocket + 快照插值 + 预测回滚 |
| TAA/Hi-Z | Q3 | 运动矢量 + Halton jitter + 遮挡剔除 |
| WebGPU | Q4 | WGSL 双轨 + compute×3（Hi-Z/DDGI/ReSTIR） |
| 资产管线接入 Worker | Q5 | GLTF/纹理/DDC 迁入 Worker，导入不冻结 |
| 多人协作 | Q6 | companion.mjs + presence + 对象锁 + CI |
| 静态导出 + 验收游戏 | Q7 | 单目录导出 + action3d v2 完整游戏 |

---

## 非功能需求

| 维度 | 要求 |
|---|---|
| **性能** | WebGL2 1080p 中端 GPU ≥30fps；Software 160p parity 通过 |
| **依赖** | 零运行时依赖 / 无 npm install / 无构建步骤 / 纯 ES2022 |
| **确定性** | 跨运行可重现（Rng 种子 / 序列化 byte-identical / 固定 key 序） |
| **计算驻留** | 渲染/物理/烘焙/推理/序列化 100% 浏览器端；companion 仅文件 IO |
| **可测试** | 全量 smoke Node headless；CPU 黄金参考永不下线；parity 断言 |
| **降级** | WebGPU 不可用时回退 WebGL2；Worker 不可用时顺序降级 |

---

## 关键设计原则

1. **单后端自包含**：非微服务，引擎+编辑器+运行时同进程
2. **CPU 黄金参考**：Software 光栅器作为 parity 基准，GPU 实现必须逐像素对齐（≤2/255）
3. **编辑器→运行时单向**：编辑器不依赖运行时，运行时不依赖编辑器
4. **诚实降级**：caps 翻真前行为保持 false，无能力时显式降级而非静默崩溃
5. **确定性可重现**：固定 key 序 / LF / 无时间戳 / 稳定 ID / 种子 Rng

---

## 测试与质量

| 指标 | 当前值 | 目标值 |
|---|---|---|
| 引擎模块 | 109 | ~130（v2 完成时） |
| Smoke 测试 | 50 | ~65 |
| 断言总数 | 1596 | ~2500 |
| 全量执行时间 | ~500ms | <2s |
| 游戏 Demo | 4 | 5（含 action3d v2） |

---

## 交付里程碑

```
v1 (已交付):  109 模块 / 50 smoke / 1596 断言 / 4 游戏 Demo / WebGL2 完整 / 物理 3D
v2 Q1-Q5 (部分): + 物理 GJK+EPA + 角色控制器 + Worker 池协议
v2 Q2-Q4 (待):  + 音频 + 网络 + TAA/Hi-Z + WebGPU compute
v2 Q6-Q7 (待):  + 多人协作 + 静态导出 + 验收游戏
```

---

## 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| WebGPU 浏览器兼容性 | 用户无法使用 compute | 诚实降级到 WebGL2；caps 翻真前保持 false |
| 物理数值稳定性 | 堆叠爆炸/穿透 | 多点接触 + Baumgarte 校正 + 休眠双阈值 + 恢复阈值 |
| Worker 浏览器限制 | Node 端不可用 | 顺序降级保持异步语义，同一套代码 |
| 性能不达标 | 游戏卡顿 | Software parity 基准 + 分阶段优化 + profiler 实测 |
| 范围蔓延 | 无法交付 | 非目标明确排除（第三方库/服务端/AAA/WebXR） |

---

## 一句话总结

**打开浏览器 → 写代码 → 调试 → 构建 → 发布 → 玩家即点即玩——对标 Unity6 的完整浏览器端游戏开发闭环，零安装、零后端、零依赖。**
